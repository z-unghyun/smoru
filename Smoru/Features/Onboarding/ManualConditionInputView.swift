import SwiftUI

struct ManualConditionInputView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Manual Condition Input",
            subtitle: "Fallback input for denied or missing HealthKit data."
        )
    }
}
