import SwiftUI

struct HistoryView: View {
    var body: some View {
        PlaceholderScreen(
            title: "History",
            subtitle: "Monthly sleep fragment collection placeholder."
        )
    }
}
