import SwiftUI

struct RoutineFocusView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Routine Focus",
            subtitle: "Full-screen focus mode placeholder."
        )
    }
}
