import SwiftUI

struct TrialHomeView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Trial Home",
            subtitle: "Sample sleep fragment and sample routine will be added later."
        )
    }
}
