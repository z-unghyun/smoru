import SwiftUI

struct TaskEditorView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Task Editor",
            subtitle: "Task add and edit placeholder."
        )
    }
}
