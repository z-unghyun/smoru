import SwiftUI

struct RoutineSetupView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Routine Setup",
            subtitle: "Routine edit placeholder."
        )
    }
}
