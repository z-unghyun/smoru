import SwiftUI

struct HealthKitPermissionStateView: View {
    var body: some View {
        PlaceholderScreen(
            title: "HealthKit Permission State",
            subtitle: "Permission status and next action placeholder."
        )
    }
}
