import SwiftUI

struct RoutineTemplateSelectionView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Routine Template Selection",
            subtitle: "Template choice placeholder."
        )
    }
}
