import SwiftUI

struct SleepReportView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Sleep Report",
            subtitle: "Daily report details placeholder."
        )
    }
}
