import SwiftUI

struct FeatureRequestView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Feature Request",
            subtitle: "Feature request form placeholder."
        )
    }
}
