import SwiftUI

struct ErrorReportView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Error Report",
            subtitle: "Error report form placeholder."
        )
    }
}
