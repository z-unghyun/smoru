import SwiftUI

struct HealthKitIntroView: View {
    var body: some View {
        PlaceholderScreen(
            title: "HealthKit Intro",
            subtitle: "Explanation before system permission request."
        )
    }
}
